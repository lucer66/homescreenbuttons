<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homescreen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".questionform"></activity>
        <activity android:name=".QuestionForm" />
        <activity android:name=".TechnInform1" />
        <activity android:name=".InformaticaOpendag" />
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CMD1" />
        <activity android:name=".Communicatie1" />
        <activity android:name=".CreativeMGT1" />
    </application>

</manifest>